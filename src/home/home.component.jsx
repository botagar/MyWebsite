import React from 'react'

const Home = (props) =>
  <div>
    <h1>Home</h1>
  </div>

Home.propTypes = {
  // Import Proptypes to start asserting props (import PropTypes from 'prop-types')
}

export default Home
